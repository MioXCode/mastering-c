#include <stdio.h>

int main()
{
    /**
     *
     * a += b
     * a -= b
     * a *= b
     * a /= b
     * a %= b
     *
     */

    int x = 6;
    int y = 10;

    int z = x + y;

    printf("%d", z);

    return 0;
};