#include <stdio.h>

int main()
{
    /**
     *
     * + ( addition )
     * - ( subtration )
     * * ( multiplication )
     * / ( division )
     * % ( modulus )
     * ++ ( increment )
     * -- ( decrement )
     *
     */

    int x = 2;
    int y = 5;

    int z = x / (float)y;

    printf("%d", z);

    return 0;
};